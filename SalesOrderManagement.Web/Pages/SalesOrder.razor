@page "/SalesOrder"
@inherits SalesOrderBase

@if (Buildings == null)
{
    <h3>Loading....</h3>
}
else
{
  int i = 1;
 <div class="row">        
<div class="col-md-4">           
 <label for="Country" class="control-label">State</label>        
</div>
  <div class="col-md-4">
   <label for="Country" class="control-label">Building</label>
 </div>
</div>    
<div class="row" style="padding-top:10px">      
  <div class="col-md-4">           
 <select class="form-control">
        <option value="">-- Select State --</option>
                @foreach (var building in Buildings)
                {
                    <option value="@building.BuildingId">@building.Name</option>
   }          
  </select>       
 </div> 
  <div class="col-md-4">           
 <select class="form-control">          
 <option value="">-- Select Building --</option>              
  @foreach (var building in Buildings)             
   {
    <option value="@building.BuildingId">@building.Name</option>
   }          
  </select>       
 </div> 
  </div>
   
    <div class="row">
        <div class="col-md-4">
            <input type="text" class="form-control" @bind="windowName" placeholder="window name" />
        </div>
        <div class="col-md-4">
            <input type="text" class="form-control" @bind="QuantityOfWindow" placeholder="Quantity Of Window" />
        </div>
        <div class="col-md-4">
            <input type="text" class="form-control" @bind="TotalSubElement" placeholder="Total Sub Element" />
            <button class="btn-primary btn-sm" @onclick="addItem">Add</button>
        </div>

   @* <table class="table-bordered">
        <tr>
            <th>SL</th>
            <th>
                Name
            </th>
            <th>
                Action
            </th>
        </tr>
        @foreach (var item in Buildings)
        {
            <tr>
                <td>
                    @(i++)
                </td>
                <td>
                    @item.Name
                </td>
                <td>
                    <button class="btn-primary btn-sm">Edit</button>
                </td>
            </tr>
        }
    </table>*@
    </div>
}




